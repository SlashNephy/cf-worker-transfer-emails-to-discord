name: Check

on:
  push:
    branches:
      - master

  pull_request:
    types:
      - opened
      - synchronize

  workflow_dispatch:

jobs:
  build:
    uses: SlashNephy/.github/.github/workflows/yarn-run.yml@2b64691f87d64d627f89946fb6f21169ddffc8b0 # master
    with:
      script: build
    permissions:
      contents: read

  lint:
    uses: SlashNephy/.github/.github/workflows/yarn-lint.yml@2b64691f87d64d627f89946fb6f21169ddffc8b0 # master
    permissions:
      contents: read
      pull-requests: write
    with:
      enable-prettier: false

  format:
    if: github.event_name != 'pull_request'
    uses: SlashNephy/.github/.github/workflows/yarn-format.yml@2b64691f87d64d627f89946fb6f21169ddffc8b0 # master
    permissions:
      contents: write
